export const locationFilterSelector = (state) => state.graphParamState.locationFilter;

export const pathFilterSelector = (state) => state.graphParamState.pathFilter;

export const graphParamSelector = (state) => state.graphParamState;
